(ns kaleidoscope.ui.components.image-thumbnail
  (:require ["react" :as react]
            ["./js/ImageThumbnail" :refer [ImageThumbnail]]))

(defn image-thumbnail
  []
  (js/console.log "THUMB" ImageThumbnail)
  ;;[thumbnail/ImageThumbnail]
  )
