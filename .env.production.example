VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=
VITE_BUGSNAG_API_KEY=
VITE_RELEASE_STAGE=production
# Versioned API namespace served by the backend (dual-mounted at root during
# migration). Must be /api/v1 so the client hits the versioned routes.
VITE_API_BASE_URL=/api/v1
